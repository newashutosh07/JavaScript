/*
arithmetic operators = operands (values, variables, etc.)
                operators (= - * /)
                ex. 11 = x + 5;
*/

let students = 30;

// students = students + 1;
// students = students - 1;
// students = students * 2;
// students = students / 2;
// students = students ** 2; //Square the number
// let extraStudents = students % 2; //gives the remainder

// students += 1;
// students -= 1;
// students *= 1;
// students /= 1;
// students **= 1;
// students **= 1;
// students %= 1;

// Increment
// students++;
// students--;

// console.log(`Number of student is ${students}`);

/*
operators precedence
1. parenthesis ()
2. exponents
3. multiplication & division & modulos
4. addition & subtraction
*/

// let result = 1 + 2 * 3 + 16;
// console.log(result);
